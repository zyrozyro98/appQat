{
  "name": "qat-web-app",
  "version": "1.0.0",
  "description": "تطبيق قات - منصة بيع وتوصيل القات",
  "main": "server.py",
  "scripts": {
    "start": "python server.py",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.9"
  }
}
